import { StyleSheet, View, Text } from "react-native";
import ComingSoon from "../../../components/ComingSoon";

const BedTimeMode = () => {
  return <ComingSoon title="Bed time mode" />;
};

export default BedTimeMode;

const styles = StyleSheet.create({
  screen: { flex: 1 },
});
